# Kruskal 最小生成树

> 模块：05 数据结构 ｜ 状态：🟦 合格回忆版真题 1/2

## 定义
Kruskal 用于带权连通无向图的最小生成树（MST）。将全图边按权值从小到大处理，若当前边连接两个不同连通分量、加入后不会成环，则选中并合并两个分量，直到成功选出 `n-1` 条边。

## 要点
- 以“边”为中心，不需要起点。
- 当前全局最小剩余边可以位于图的任意位置，只要不成环即可加入。
- 成环判断本质是判断两个端点是否已属于同一连通分量。
- 常配合并查集：`find(u) != find(v)` 时选边并 `union(u,v)`。
- n 个顶点选够 `n-1` 条边即可停止。
- 典型复杂度 `O(E log E)`，通常更适合稀疏图。
- 与 Prim 一样属于贪心算法。

## 易错点
- “顶点以前已经出现过”不等于“两个端点已经在同一连通分量”。两个不同小树之间的边应当允许加入。
- 不能只因边权最小就选，还必须检查是否成环。
- 不要套用 Prim 的“只能树内→树外”规则。

## 本轮训练
- 基础题前 4 题正确。
- 综合题首次把 `B-C=3` 错过，错误地选 `A-C=4`；薄弱点为“两个已出现顶点仍可能属于不同连通分量”。
- 针对性补强：`{A,B}` 与 `{C,D}` 之间的 `B-C` 应选；后续“C-D 选、A-C 跳过”判断正确；综合确认题正确得到 `A-B=1, C-D=2, B-C=3, D-E=5`，总权值 11。
- 2023 年上半年软件设计师网友回忆版第 64～65 题：Kruskal 策略答 C（贪心），按图计算 MST 总权值答 A（14），均正确。

## 我的笔记
- 无新增个人记忆点。

## 校准状态
- 基础题组经针对性补强达标。
- 合格回忆版真题 1/2。
- 待第 2 道合格回忆版真题，保持 🟦。
