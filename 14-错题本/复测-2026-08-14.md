# 2026-08-14 到期错题复测

> 本文件记录 2026-08-14 当日换题复测过程；完成后同步汇总到错题本与进度跟踪。

## #错1｜浮点 IEEE754 · 从编码求真值
- 复测题：单精度 `1 10000011 00100000000000000000000` 求十进制真值。
- 用户作答：符号负；`131-127=4`；尾数 `1.125`；`-2^4×1.125=-18`。
- 判定：✅ 正确。
- 当前连续换题答对：1/2。
- 后续：保留待再次换题复测，未归档。

## #错2｜浮点 IEEE754 · 概念（隐含1/偏移/0的表示）
- 复测题：判断单精度规格化数的显式尾数位数、偏移阶码、隐藏位与 `+0` 阶码字段。
- 用户作答：A 错（还有隐藏的 1）；B 对；C 错（隐藏位不存）；D 错（`+0` 阶码字段全 0）。
- 判定：✅ 正确。
- 连续换题答对：2/2。
- 后续：✅ 达到归档条件；本次复测收尾时同步错题总表。

## #错4｜流水线冲突解决 + 超标量/超流水线
- 复测题：识别同周期多发射、阶段细分、数据转发、分支预测四种技术及其主要作用。
- 用户作答：超标量→提升流水线效率；超流水线→提升流水线效率；数据转发→解决数据冲突；分支预测→解决控制冲突。
- 判定：✅ 正确。补充：超标量核心是同周期多发射、提高指令级并行度/吞吐率；超流水线核心是阶段细分、提高时钟频率。
- 当前连续换题答对：1/2。
- 后续：保留待再次换题复测，未归档。

## #错22｜死锁检测 · 暂时资源不足不等于死锁
- 复测题：`Work=(1,0)`，P1 `Request=(1,1), Allocation=(1,0)`；P2 `Request=(1,0), Allocation=(0,1)`；P3 `Request=(2,0), Allocation=(1,1)`，执行死锁检测。
- 用户作答：不能因 P1 暂时不满足直接判死锁；先完成 P2；最终无死锁；完成顺序 `2→1→3`；Work 变化 `11→21→32`（即 `(1,1)→(2,1)→(3,2)`）。
- 判定：✅ 正确。
- 连续换题答对：2/2。
- 后续：✅ 达到归档条件；本次复测收尾时同步错题总表。

## #错23｜死锁解除 · 回滚与一致检查点
- 复测题：转账事务执行完“A 扣款 500”后因死锁解除被剥夺资源，判断应从断点继续还是回滚。
- 用户作答：C；当前检查点不一致，需要回滚修改；转账这样的操作应保持原子性。
- 判定：✅ 正确。
- 连续换题答对：2/2。
- 后续：✅ 达到归档条件；本次复测收尾时同步错题总表。
