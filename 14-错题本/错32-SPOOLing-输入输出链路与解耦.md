# [#错32] SPOOLing · 输入/输出链路与进程解耦

- 出处：仿真校准题（2026-08-14）
- 题干（简）：判断 SPOOLing 的输入井、输出井、输入进程、输出进程及用户进程等待关系；四项中选错误说法。
- 我的答案：B，并认为“井里面内容发送给实际设备”。
- 正确答案：D。B“输入进程负责把输入设备的数据送入输入井”是正确的；C“输出进程负责把输出井中的任务送给实际输出设备”也是正确的。用户进程将任务提交到输出井后通常可以继续执行，不必等待物理打印完成。
- 错因：把输入链路与输出链路方向混淆，并把“从井送往实际设备”的输出过程误套到输入进程。
- 纠正规则：输入链路=`输入设备 → 输入进程 → 输入井`；输出链路=`用户/作业 → 输出井 → 输出进程 → 输出设备`。看到“提交后是否必须等待物理设备完成”时，SPOOLing 的核心是解耦，通常不必等待。
- 关联知识点：操作系统 / 设备与作业 / SPOOLing 原理与组成
- 错因类型：概念+方向映射
- 复习计划：D+1 2026-08-15 ｜ D+3 2026-08-17 ｜ D+7 2026-08-21 ｜ D+14 2026-08-28
- 当场补强 1：2026-08-14，正确补全 `输入设备 → 输入进程 → 输入井` 与 `输出井 → 输出进程 → 打印机`，并明确用户进程提交输出井后无需等待打印完成，✅。
- 当场补强 2：2026-08-14，批量输入链路前半正确；输出链路仍写成“输出设备，输出井”，再次混淆“先进入输出井，再由输出进程送往设备”，❌。
- 重复错因处理：停止继续堆叠同型填空，改用事件/中转站模型补讲后再换形式验证。
- 换形式验证 1：2026-08-14，将打印事件排序为 `用户提交(3) → 进入输出井(4) → 输出进程取出(2) → 打印机执行(1)`，回答 `3421`，✅。
- 换形式验证 2：2026-08-14，场景题“打印机正忙时，用户进程 P 提交新打印任务”，选择 B：`P → 输出井；输出进程择机 → 打印机`，✅。
- 当前状态：补讲后连续 2 道不同形式验证正确，当场补强通过；不计延迟复测，D+1 仍按 2026-08-15 执行。
- 归档：⬜（延迟复测连续换题答对 0/2）
