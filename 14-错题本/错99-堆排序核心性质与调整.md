# 错99：堆排序核心性质与调整

- 日期：2026-09-15
- 分类：数据结构 / 排序 / 堆排序
- 状态：🟦 当场补强通过，待延迟复测

## 错因
1. 向下调整交换后未跟踪下沉元素的新位置。
2. 把恢复大根堆误当成把剩余元素直接排成升序。
3. 原地堆排序额外空间误判；正确 O(1)。
4. 稳定性误判；堆排序不稳定。
5. 0 基数组最后一个非叶子结点误判；正确 floor(n/2)-1。
6. 自底向上建堆复杂度误判；正确 O(n)，完整堆排序 O(n log n)。

## 当场补强
后续能正确完成多组堆顶交换和向下调整；正确判断 n=8 最后一个非叶子结点为 3、n=15 为 6；正确区分建堆 O(n) 与完整排序 O(n log n)；稳定性、空间复杂度综合题通过。

## 复测要求
同日补强不计延迟通过；后续用不同数据进行换题复测。
