# 2026-09-14 · 数据结构：快速排序

- 今日内容：pivot/一趟划分、挖坑法、平均/最坏时间复杂度、递归栈空间复杂度、稳定性。
- 基础表现：能判断一趟划分后 pivot 就位；能独立走一遍挖坑法；能区分单趟 `O(n)` 与完整快排平均 `O(n log n)`。
- 真题校准：完成 2020 下半年快速排序划分题；掌握以末元素为 pivot 的划分过程，结果为 `2,3,1,4,7,5,6,8`，单趟复杂度 `O(n)`。
- 新增错题：#错97。曾把平均时间误选 `O(log n)`，把平均栈空间误选 `O(n)`；随后多题补强通过。
- 个人记忆点：无新增。
- 状态：🟦，基础与校准完成，待延迟复测。
- 下一步：继续排序下一个最小子考点；开始新授前优先处理到期错题。
