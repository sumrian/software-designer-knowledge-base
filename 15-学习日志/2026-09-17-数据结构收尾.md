# 2026-09-17 · 数据结构主线收尾

## 遗留校准清仓
完成 B/B+ 树、图存储、DFS/BFS、Prim/Kruskal/Dijkstra、拓扑/AOV、分块查找、哈希链地址法的收尾校准与 10 题综合检查。

关键通过点：B+ 树叶子链与范围查询；B 树插入分裂/删除借关键字；稀疏图邻接表；BFS 队列/DFS 栈递归；Prim/Kruskal 最小生成树；Dijkstra 非负权单源最短路；拓扑排序入度 0 与有向环判断；链地址法 alpha 可大于 1 且最坏可 O(n)。

## 回退
#错88 分块查找复杂度隔日回退：曾再次把“索引折半 + 块内顺序”整体误判为 O(log n)。随后补强并在综合检查中正确答 O(sqrt(n))，但重新进入延迟复测队列。

## 章节状态
数据结构计划内新授与首次校准主线完成。活动错题与延迟复测继续跨章节按日期穿插，不阻塞进入下一章节。
